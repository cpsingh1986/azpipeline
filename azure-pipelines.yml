trigger: none 
name: $(BuildDefinitionName)_$(date:yyyyMMdd)$(rev:.r)

extends:
  template: pipelines/service.pipeline.stages.yaml@self
  parameters:
    enviornmentPrefix: EventStore
    componentName: EventStore_Api

